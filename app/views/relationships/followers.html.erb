<hr>
<h4 class="text-center"><b>フォロワー一覧</b></h4>
<hr>

<% if @users.exists? %>
  <% @users.each do |user| %>
    <%= image_tag user.get_profile_image(100,100), class: "rounded-circle" %>
    <%= user.name %>
  <% end %>
<% else %>
  <p>まだ誰からもフォローされていません。</p>
  <p>公開投稿を増やしていきましょう！</p>
<% end %>

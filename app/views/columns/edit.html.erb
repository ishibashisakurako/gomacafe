<hr>
<h4 class="text-center"><b><i class="fa-solid fa-wand-magic-sparkles"></i> コラム編集</b></h4>
<hr>
<div class="row">
  <div class="col-md-8 offset-2">
<%= render 'columns/form', columns: @columns %>
<div class="mt-2">
<% if @column.user == current_user %>
  <%= link_to "削除する", column_path(@column), method: :delete, class: "btn btn-outline-danger", data: {confirm: "投稿を削除しますか？"} %>
<% end %>
</div>
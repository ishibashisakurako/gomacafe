<hr>
<div class="row">
  <div class="col-md-3 offstt-md-1">
    <div class="text-center">
      <div class="mozi font-english">
        <i class="fa-solid fa-user user-btn"></i> Author
      </div>
      <hr>
      <%= image_tag @column.user.get_profile_image(100,100), class: "rounded-circle" %>
      <p class="mozi"><%= link_to @column.user.name, user_path(@column.user.id) %></p>
      <hr>
    </div>
  </div>
  <div class="col-md-8">
    <div class="text-center mozi">
      <i class="fa-solid fa-feather tag-btn"></i> コラム：<%= @column.title %>
      <% if @column.user == current_user %>
        <%= link_to edit_column_path(@column), class: "btn btn-outline-success" do %>
          <i class="fa-solid fa-gear"></i>
        <% end %>
      <% end %>
    </div>
    <hr>
    <%== @column.body %>
    <style>
      figcaption {
        display: none!important;
      }
      figure img {
        max-width: 50%;
        object-fit: contain;
      }
    </style>
  </div>
<div>